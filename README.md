The file "generate_gif_protocol" will generate a gif where the entire LED pannel will display red light. Whereas, the file "generate_gis_protocol_delayed" will generate a gif where the two half of the LED pannel display the protocol with a delay (time of the delay can be modified). 
The delay feature is useful to conduct two optogenetic experiments. 
